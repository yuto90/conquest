# Conquest Isles トップ画面の比較・検証 — 2026年9月13日

**最終結果：合格**

元画像との比較で見つかったロゴの影・文字幅・小画面の読みやすさを修正しました。対応が必要な重大度 P0〜P2 の指摘は残っていません。

## 比較対象と画像

- 元画像：[ユーザー提供のデザイン](assets/title/title_reference.png)（941 × 1672ピクセル）。提供時のファイル名は `conquest_isles_top.png`。
- 実装：[トップ画面](lib/ui/title_screen.dart)。
- 同じサイズで描画した画面：[最終版](design-qa-assets/isles-title/reference-size-final.png)。
- 全体と操作部分を並べた画像：[元画像と実装の比較](design-qa-assets/isles-title/comparison-final.png)。
- ブラウザでの表示：[390 × 844](design-qa-assets/isles-title/web-title-390.png)、[320 × 568](design-qa-assets/isles-title/web-title-320.png)、[1440 × 900](design-qa-assets/isles-title/web-title-wide.png)。

![元画像と実装の比較](design-qa-assets/isles-title/comparison-final.png)

## 比較条件

- 元画像と実装の画像は、ともに941 × 1672ピクセルです。実装は同じ論理サイズのFlutter画面を `toImage(pixelRatio: 1)` で描画しました。同梱フォントと画像の読み込み後に、通常の影を有効にして撮影しています。
- 比較画像では、元画像と実装を50%の大きさで横に並べ、下段にロゴ・キャッチコピー・ボタン部分を同じ範囲で切り出しています。Codexのアプリ内ブラウザで、表示領域977 × 1196、デバイスピクセル比1として確認しました。
- 比較時の状態は、起動直後のタイトル画面です。ダイアログは閉じ、ボタンにマウスポインターを重ねていません。
- Web版は既存の390:844の縦長表示領域を維持しています。小画面・横長画面では、この比率に応じた収まりを確認しました。地図は背景全体を覆い、ロゴは元の縦横比を保ちます。

## 確認結果

| 観点 | 確認した内容 |
| --- | --- |
| 文字・書体 | `CONQUEST ISLES` は元画像の字形と影を利用しています。`START` はBarlow SemiBold、キャッチコピーと下段ボタンはLibre Baskerville Boldを使用し、太さ・文字間隔・文字幅を調整しました。フォントとOFLライセンスを同梱しています。 |
| 配置・余白 | 幅941ピクセルでは、ロゴの切り出し位置をy=666、開始ボタンを幅667・y≈898、下段ボタンの並びを幅562・y≈1081に合わせています。操作領域の高さは48論理ピクセル以上です。小画面では下段ボタンの幅を広げ、文字拡大時は縦並び、画面の高さが足りない場合はスクロールで対応します。 |
| 色・質感 | 青緑の海図、濃紺のロゴと操作部分、白い開始ボタンの文字、細い枠線と影を合わせています。背景は元画像を編集して島・コンパス・格子・航路を残し、ロゴ周辺の青緑色は描画時に透過処理しています。 |
| 画像 | 地図は元の解像度のPNG、ロゴは元画像の範囲を指定して表示しています。ボタン・文字・読み上げ・フォーカス表示・ダイアログはFlutterの部品として実装しています。 |
| 表示文言 | `CONQUEST ISLES`、`A STRATEGY FOR A WIDER WORLD`、`START`、`SETTINGS`、`HOW TO PLAY` を元画像に合わせています。ダイアログと操作の読み上げは日本語・英語に対応します。 |
| アイコン・操作 | スピーカー・歯車・本にはCupertino／Materialのアイコンを使用しています。開始ボタンと2つの設定ボタンから既存の対戦設定を開き、遊び方ボタンから説明を表示できます。 |

## 比較で見つかった差と修正

1. **P2：ロゴの影が薄く、下段ボタンの文字が横に広い。**
   [初回の描画](design-qa-assets/isles-title/reference-size-first.png)を確認し、透過処理で不透明に保つ色の範囲を広げ、下段文字の幅と歯車アイコンを調整しました。同じサイズで再描画した[最終比較](design-qa-assets/isles-title/comparison-final.png)で修正を確認しました。初回の撮影ではテスト環境で影のぼかしが無効になっていたため、最終画像では通常の影を有効にしています。
2. **P2：小画面で下段ボタンの文字が縮みすぎる。**
   ブラウザを320 × 568にすると、Web版の縦長領域が幅約262ピクセルになり、下段の文字が小さくなっていました。下段ボタンの並びに最小幅を設け、幅941ピクセルの配置を保ちながら読みやすさを確保しました。再ビルド後の[320 × 568](design-qa-assets/isles-title/web-title-320.png)と[390 × 844](design-qa-assets/isles-title/web-title-390.png)で、文字と操作部分の収まりを確認しました。最終コードレビューでも修正を確認しています。

## 操作・実装の検証

- アプリ内ブラウザで「START → 対戦設定 → 8島を選択 → タイトルへ戻る → 右上の設定 → 8島の選択が保持されていることを確認 → カウントダウン → 対戦開始」を確認しました。
- [遊び方](design-qa-assets/isles-title/web-help.png)は日本語で表示され、対戦を開始せずタイトルへ戻れます。サウンドの案内も開閉できます。
- [対戦開始後の画面](design-qa-assets/isles-title/web-playing.png)を確認しました。画面遷移後と最終ビルドの確認時に、ブラウザの警告・エラーは検出されませんでした。
- Flutterの全195テストが成功しました。設定保持、タイトル遷移、画面端の安全領域、文字拡大、アプリの一時停止・復帰、対戦動作を含みます。タイトル画面は9テストで、設定ボタンと遊び方の操作テストを2件追加しています。
- `fvm flutter analyze --no-pub`：問題なし。
- `fvm flutter build web --release --no-pub`：成功。
- `fvm flutter build ios --simulator --debug --no-pub`：成功。この検証では、起動したiOSシミュレータ上での操作確認は行っていません。
- 最終コードレビュー：小画面の修正後、対応が必要な重要指摘なし。

## 残る軽微な差と制約

- **P3：アイコンと表面の質感。** 歯車の歯数や本の線は、元画像とライブラリのアイコンでわずかに異なります。開始ボタンは濃紺の単色と影で表現し、元画像にあるごく薄い色むらは再現していません。編集した背景にも細かな質感の差があります。
- 既存アプリには音楽・効果音がありません。サウンドアイコンを押すと、その対応状況を案内します。
- 元画像にないフォーカス、マウスポインターを重ねた状態、ダイアログ、読み上げは、既存Flutterアプリの操作に合わせています。

## 完了項目

- [x] 指定画像を既存Flutterアプリのトップ画面に再現。
- [x] 地図・ロゴの元画像・フォント・ライセンスを同梱。
- [x] 操作を接続し、設定保持とアプリの一時停止・復帰を維持。
- [x] 全体と細部を同じ条件で比較し、P2の差を修正。
- [x] 小画面・横長画面、ブラウザ操作、テスト、解析、ビルドを確認。

<!-- final result: passed -->
